
pub class Main {
    static fn factorial(n: i32) i32 {
        var total = 1;
        while (n > 0) : (n -= 1) {
            total *= n;
        }
        return total;
    }

    pub static fn main(args: [String]) void {
        System.out.println("Factorial 10 = " + factorial(10));
    }
}
