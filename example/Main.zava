pub class Main {
    pub static fn main(args: [String]) void {
        var i = test();
        while (i > 0) : (i -= 1) {
            System.out.println("Hello, Zava!");
        }
    }

    fn test() i32;
}

